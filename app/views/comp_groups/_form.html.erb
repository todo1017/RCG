<%= form_for(@comp_group) do |f| %>
  <% if @comp_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comp_group.errors.count, "error") %> prohibited this comp_group from being saved:</h2>

      <ul>
      <% @comp_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
    <div class="actions">
      <br>
      <%= f.submit "Save", {:class=>"btn btn-success"} %>
      <%= link_to "cancel", "/comp_groups" %>
    </div>
<% end %>
