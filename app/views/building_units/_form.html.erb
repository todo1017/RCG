<%= form_for(@building_unit) do |f| %>
  <% if @building_unit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@building_unit.errors.count, "error") %> prohibited this building_unit from being saved:</h2>

      <ul>
      <% @building_unit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <%= f.hidden_field :building_id, value: @building_unit.building_id %>
    <%= f.hidden_field :bed_bath, value: "owner_record" %>
    <div class="actions">
      <br>
      <%= f.submit "Save", {:class=>"btn btn-success"} %>
      <%- if @building_unit.building_id == nil then path = root_path else path = "/rent_roll/#{@building_unit.building_id.to_i}" end  %>
      <%= link_to "cancel", path %>
    </div>
    <br>
    <div class="field">
      <br>
      <i>Concessions...</i>
      <br>
      <br>
      <%= f.label :months_off %><br>
      <%= f.text_field :months_off %>
    </div>
    <div class="field">
      <%= f.label :cash_off %><br>
      <%= f.text_field :cash_off %>
    </div>
    <div class="field">
      <%= f.label :lease_length %><br>
      <%= f.text_field :lease_length %>
    </div>

<% end %>
